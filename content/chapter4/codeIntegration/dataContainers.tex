\subsection{Data Containers}
Due to the fact that handling memory dynamically in c or c++ (unlike with Java's automatic garbage collection) the most data containers are allocated before the main API call this includes principle component analysis specific variables but also sliding window and tde stuff. Before calling the main API following variables have to be defined with the same or similar variable name to prevent confusion.
\lstinputlisting[
  language=C,
  caption={FILL OUT!. The complete source code is listed in the Appendix~\ref{app:simplelinearregression}.\\~},
  label={src:linregressionalgorithm},
  firstnumber=44,
  firstline=44,
  lastline=70
]{content/codebase/code/anomaly_detection.cpp}